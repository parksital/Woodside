type Venue @model {
  id: ID!
  name: String!
  events: [Event] @connection(name: "VenueEvents")
  description: String
}
type Event @model {
  id: ID!
  name: String!
  date: AWSDateTime!
  venue: Venue @connection(name: "VenueEvents")
  description: String
}